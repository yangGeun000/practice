<link rel="stylesheet" href="@{/bootstrap.min.css}">
<div class="container my-3">
    <h2 class="border-bottom py-2">[[${question.subject}]]</h2>
    <div class="card my-3">
        <div class="card-body">
            <div class="card-text" style="white-space: pre-line;">[[${question.content}]]</div>
            <div class="d-flex justify-content-end">
                <div class="badge bg-light text-dark-p-2 text-start">
                    [[${#temporals.format(question.createDate,'yyyy-MM-dd HH:mm')}]]
                </div>
            </div>
        </div>
    </div>
    <h5 class="border-bottom my-3 py-2">[[${question.answerList.size()}]]개의 답변이 있습니다.</h5>
    <div class="card my-3" th:each = "answer : ${question.answerList}">
        <div class="card-body">
            <div class="card-text" style= "white-space: pre-line;">[[${answer.content}]]</div>
        </div>
        <div class="d-flex justify-content-end">
            <div class="badge bg-light text-dark p-2 text-start">
                [[${#temporals.format(answer.createDate,'yyyy-MM-dd HH:mm')}]]
            </div>
        </div>
            <!-- <button th:onclick="|location.href='@{|/answer/delete/${answer.id}|}'|">삭제</button> -->
    </div>
    <form th:action="@{|/answer/create/${question.id}|}" method="post" class="my-3">
        <textarea name="content" id="content" rows="15" class="form-control"></textarea>
        <input type="submit" value="답변등록" class="btn btn-primary my-2">
    </form>
</div>